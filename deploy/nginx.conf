server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml font/woff2;
    gzip_min_length 1024;

    include /etc/nginx/snippets/security-headers.conf;

    # hashed filenames, so a long cache is safe
    location /assets/ {
        include /etc/nginx/snippets/security-headers.conf;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # the shell and the worker name no hash: cache either one and the user is
    # stuck on the build that was current when they last visited
    location = /index.html {
        include /etc/nginx/snippets/security-headers.conf;
        add_header Cache-Control "no-cache";
    }

    location = /sw.js {
        include /etc/nginx/snippets/security-headers.conf;
        add_header Cache-Control "no-cache";
    }

    # the public introduction, reachable without the .html
    location = /about {
        include /etc/nginx/snippets/security-headers.conf;
        add_header Cache-Control "no-cache";
        try_files /about.html =404;
    }

    # single page app, any unknown path falls back to the shell
    location / {
        try_files $uri $uri/ /index.html;
    }
}
